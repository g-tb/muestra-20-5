# Muestra

Recuerden que para Copilot, el AGENTS.md default se genera en `.github/copilot-instructions` cuando corremos `/init`, si comparten el proyecto con compañeros que usan otras tools como Codex o Claude Code, recomendamos crear el directorio `.agents` y un `AGENTS.md`, el que podemos referenciar desde `copilot-instructions`.  
